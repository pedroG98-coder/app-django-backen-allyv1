from django.urls import include, path, re_path
from rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView

from land import views

urlpatterns = [
    path("", views.index, name="index"),


    # ==========================================================================================================================
    # Endpoint Token
    # ==========================================================================================================================
    path(
        "api/token/",
        views.MyTokenObtainPairView.as_view(),
        name="token_obtain_pair",
    ),


    # ==========================================================================================================================
    # Endpoint Token Refresh
    # ==========================================================================================================================

    path(
        "api/token/refresh/",
        TokenRefreshView.as_view(),
        name="token_refresh",
    ),


    # ==========================================================================================================================
    # Endpoint POST Usuario
    # ==========================================================================================================================


    path(
        "usuario",
        views.UsuarioLista.as_view(),
        name="usuario-list",
    ),


    # ==========================================================================================================================
    # Endpoint GET Usuario
    # ==========================================================================================================================


    path(
        "usuario/<int:pk>",
        views.UsuarioDetalle.as_view(),
        name="usuario-detail",
    ),


    # ==========================================================================================================================
    # Endpoint POST Tarea
    # ==========================================================================================================================


    path(
        "tarea",
        views.TareaLista.as_view(),
        name="tarea-list",
    ),


    # ==========================================================================================================================
    # Endpoint GET Tarea
    # ==========================================================================================================================


    path(
        "tarea/<int:pk>",
        views.TareaDetalle.as_view(),
        name="tarea-detail",
    ),


    path(
        "helpers/get-tiempo-restante/<int:tarea_id>/",
        views.GETTiempoRestante.as_view(),
        name="get-tiempo-restante"
    ),
]
